╔══════════════════════════════════════════════════════════════╗
║          OPERATION BLACK VAULT — PHANTOM COMMAND            ║
║                    MISSION BRIEFING                         ║
╚══════════════════════════════════════════════════════════════╝

CLASSIFICATION: TOP SECRET // SPECIAL COMPARTMENTED
OPERATION CODENAME: MERSENNE TWISTER
CATEGORY: Cryptography
DIFFICULTY: 🔴 HARD | POINTS: 700

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTELLIGENCE REPORT:

  The Vault generates its session tokens using Python's
  built-in random module — the Mersenne Twister (MT19937).

  They think this is unpredictable. It has not been
  cryptographically secure since its publication in 1998.

  We have captured 624 consecutive 32-bit outputs from
  their token stream. That is exactly enough to clone
  the internal state and predict every future output.

  Clone the RNG. Forge the next token. Enter the Vault.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MISSION OBJECTIVE:

  Recover the internal MT19937 state from 624 observed
  outputs and predict the next value to forge a valid
  session token and capture the flag.

FILES PROVIDED:
  → tokens.txt    (624 captured random token values)

PHANTOM COMMAND ACTUAL — OUT.
