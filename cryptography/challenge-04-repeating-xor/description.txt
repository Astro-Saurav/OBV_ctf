╔══════════════════════════════════════════════════════════════╗
║          OPERATION BLACK VAULT — PHANTOM COMMAND            ║
║                    MISSION BRIEFING                         ║
╚══════════════════════════════════════════════════════════════╝

CLASSIFICATION: SECRET
OPERATION CODENAME: REPEATING XOR
CATEGORY: Cryptography
DIFFICULTY: 🟡 MEDIUM | POINTS: 450

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTELLIGENCE REPORT:

  An enemy transmission was encrypted with a repeating-key
  XOR cipher. They were clever enough to use a multi-byte
  key. They were not clever enough to vary it.

  We have the plaintext header of the message — standard
  operation preamble, always the same.

  XOR the known plaintext against the ciphertext.
  Watch the key reveal itself.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MISSION OBJECTIVE:

  Use a known-plaintext attack to recover the repeating XOR
  key and decrypt the full transmission to get the flag.

FILES PROVIDED:
  → ciphertext.bin   (Encrypted transmission)

PHANTOM COMMAND ACTUAL — OUT.
