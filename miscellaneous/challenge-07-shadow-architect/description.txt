╔══════════════════════════════════════════════════════════════╗
║          OPERATION BLACK VAULT — PHANTOM COMMAND            ║
║                    MISSION BRIEFING                         ║
╚══════════════════════════════════════════════════════════════╝
CLASSIFICATION: TOP SECRET // SPECIAL COMPARTMENTED
OPERATION CODENAME: SHADOW ARCHITECT
CATEGORY: OSINT Heavy / Hybrid (Forensics + Reverse + Pwn + Web)
DIFFICULTY: 🔴 HARD | POINTS: 700
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INTELLIGENCE REPORT:
A rogue APT group known as "Vanguard-9" has deployed a command-and-control (C2)
infrastructure. They have left virtually zero traces on the target host—except 
for a core memory dump (`core.dmp`), an intercepted binary ELF file (`agent_v2`), 
and a brief Wireshark capture (`traffic.pcap`).

Phantom Command needs you to trace the operative behind this binary, discover 
their physical location, reverse their encryption keys, and compromise their 
internal live target web console to exfiltrate the operational flag.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MISSION OBJECTIVE: 
Execute a 5-Stage Multi-Domain Cyber Investigation.

[STAGE 1: DIGITAL FORENSICS & REVERSE ENGINEERING]
Analyze `core.dmp` and `agent_v2`. 
1. The memory dump contains a corrupted Linux heap state. Repair the chunk headers 
   to extract an encrypted string buffer containing a PGP key ID and an IP address.
2. Decompile `agent_v2`. The binary uses a custom obfuscated Control Flow Flattening
   (CFF) machine. Reconstruct the control flow graph (CFG) to uncover the hardcoded
   domain name format used for fallback communications: `[hash].phantom-vault.net`.

[STAGE 2: ADVANCED OSINT & GEOLOCATION]
Using the extracted PGP Key ID and domain format:
1. Search public keyservers for the developer's identity. The key signature will
   point to a personal GitHub / Codeberg repository.
2. Analyze the commit history of the developer's public dots/config repository. 
   Inspect a deleted commit containing an EXIF-tagged background wallpaper image.
3. Perform Geolocation / OSINT on the image:
   - Identify the exact latitude and longitude of the building shown in the photo.
   - Calculate the BSSID / Wi-Fi SSID broadcasted near that physical coordinate using 
     public wireless positioning databases (e.g., Wigle.net).

[STAGE 3: WEB EXPLOITATION & DYNAMIC OSINT SEEDING]
1. Locate the live target web panel hosted at the IP extracted from Stage 1.
2. The authentication mechanism accepts a dynamic TOTP token. The seed for the 
   TOTP algorithm is **NOT** static—it is generated by taking:
   `HMAC-SHA256( BSSID_From_Stage_2 , Developer_PGP_Fingerprint )`
3. Generate the correct TOTP code to bypass MFA and log into the admin dashboard.

[STAGE 4: BINARY EXPLOITATION (REMOTE PWN)]
1. Inside the admin panel, you will find a diagnostic ping utility connected to a 
   custom 64-bit ELF service running on port 1337.
2. Analyze the service: It suffers from an off-by-one stack-based buffer overflow
   vulnerability in its log parsing logic.
3. Bypasses required:
   - ASLR is ENABLED. Leak the libc address via the web diagnostic log output.
   - Craft a ROP chain (`pop rdi; ret`) using the leaked libc base to execute `/bin/sh`.

[STAGE 5: RECOVER THE FLAG]
Read `/flag.txt` on the remote server.

FILES PROVIDED:
→ core.dmp (Process memory dump with corrupted heap structure)
→ agent_v2 (Striped 64-bit ELF with flattened control flow)
→ traffic.pcap  (Captured C2 handshake attempt)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHANTOM COMMAND ACTUAL — OUT.
