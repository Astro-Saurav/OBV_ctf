╔══════════════════════════════════════════════════════════════╗
║          OPERATION BLACK VAULT — PHANTOM COMMAND            ║
║                    MISSION BRIEFING                         ║
╚══════════════════════════════════════════════════════════════╝

CLASSIFICATION: TOP SECRET
OPERATION CODENAME: TERMINAL ECHO
CATEGORY: Miscellaneous
DIFFICULTY: 🔴 HARD | POINTS: 700

LIVE SERVER: nc -4 terminal.theobv.xyz 9004

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTELLIGENCE REPORT:

  The Black Vault runs an internal Python terminal that
  executes operator commands in real-time. Thinking they
  were clever, they deployed a firewall with a strict
  regex filter.

  It blocks every letter. Every number. a-z. A-Z. 0-9.
  All of it. Completely banned.

  What they don't know is that Python speaks languages
  older than ASCII. Unicode mathematical symbols — when
  fed through Python's NFKC normalizer — collapse back
  into standard ASCII at the interpreter level.

  They built a wall of letters. You have a library of
  ten thousand glyphs that look like nothing — and
  execute as everything.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MISSION OBJECTIVE:

  Bypass the alphanumeric Python jail using Unicode
  normalization (PEP 3131) to execute arbitrary code
  and read the flag from /home/ctf/flag.txt

Connect:
  nc -4 terminal.theobv.xyz 9004

PHANTOM COMMAND ACTUAL — OUT.
