# Python dependencies — Operation Black Vault CTF
# Install with: pip install -r requirements.txt

# --- Binary Exploitation ---
pwntools>=4.12.0          # Exploit development framework (pwn)

# --- Cryptography ---
pycryptodome>=3.20.0      # AES, RSA, and other crypto primitives
gmpy2>=2.1.5              # Fast arbitrary-precision arithmetic (RSA attacks)
sympy>=1.12               # Symbolic math (factorisation, number theory)

# --- Web Exploitation ---
requests>=2.31.0          # HTTP client for web challenge exploit scripts

# --- Reverse Engineering / Misc ---
z3-solver>=4.12.0         # SMT solver (constraint solving, crackme challenges)

# --- Utilities ---
pyyaml>=6.0.1             # YAML parsing (CTFd export files)
colorama>=0.4.6           # Colored terminal output in scripts
tqdm>=4.66.0              # Progress bars in long-running scripts
