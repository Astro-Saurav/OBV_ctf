╔══════════════════════════════════════════════════════════════╗
║          OPERATION BLACK VAULT — PHANTOM COMMAND            ║
║                    MISSION BRIEFING                         ║
╚══════════════════════════════════════════════════════════════╝

CLASSIFICATION: TOP SECRET
OPERATION CODENAME: THE GHOST VM
CATEGORY: Reverse Engineering
DIFFICULTY: 🔴 HARD | POINTS: 700

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTELLIGENCE REPORT:

  The Black Vault does not trust x86 for its most sensitive
  operations. They built their own virtual machine — a
  proprietary architecture with a custom instruction set
  that no standard disassembler understands.

  We extracted the VM engine and the bytecode payload it
  executes. The engine interprets the bytecode. The bytecode
  contains the secret.

  Reverse the VM. Understand the ISA. Decompile the bytecode.
  There is no shortcut. There is no plugin for this.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MISSION OBJECTIVE:

  Reverse engineer the custom VM architecture and decompile
  the bytecode to find the correct access code.

  Usage: ./ghost_vm.elf <access_code>

FILES PROVIDED:
  → ghost_vm.elf       (The custom VM interpreter)
  → vault_logic.bin    (Encrypted bytecode payload)

PHANTOM COMMAND ACTUAL — OUT.
