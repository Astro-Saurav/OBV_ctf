╔══════════════════════════════════════════════════════════════╗
║          OPERATION BLACK VAULT — PHANTOM COMMAND            ║
║                    MISSION BRIEFING                         ║
╚══════════════════════════════════════════════════════════════╝

CLASSIFICATION: TOP SECRET // SPECIAL COMPARTMENTED
OPERATION CODENAME: DEEP ROOT
CATEGORY: Reverse Engineering
DIFFICULTY: 🔴 VERY HARD | POINTS: 700

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTELLIGENCE REPORT:

  The most sophisticated piece of malware we have ever
  encountered from the Black Vault. It has no hardcoded
  password. No static decryption key.

  It reads the host's own system logs to derive its
  decryption key at runtime — environmental keying.
  The binary and the host are inseparable. Without the
  exact log state it expects, the binary is an inert brick.

  We recovered the daemon and a memory dump of the
  log file it was accessing at the moment of capture.

  The key is inside those logs. Find it.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MISSION OBJECTIVE:

  Reverse the backdoor daemon's environmental keying
  algorithm and reconstruct the decryption key from
  the captured log file to retrieve the flag.

FILES PROVIDED:
  → backdoor_daemon.elf    (The malware binary)
  → recovered_sys.log      (Memory dump of the log file)

PHANTOM COMMAND ACTUAL — OUT.
