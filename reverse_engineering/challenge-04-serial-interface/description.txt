╔══════════════════════════════════════════════════════════════╗
║          OPERATION BLACK VAULT — PHANTOM COMMAND            ║
║                    MISSION BRIEFING                         ║
╚══════════════════════════════════════════════════════════════╝

CLASSIFICATION: SECRET
OPERATION CODENAME: SERIAL INTERFACE
CATEGORY: Reverse Engineering
DIFFICULTY: 🟡 MEDIUM | POINTS: 450

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTELLIGENCE REPORT:

  An encrypted field radio requires a hardware serial number
  to unlock its transmission mode. The operator manual was
  destroyed. The only thing that survived was a dump of the
  radio's verification firmware.

  The firmware doesn't store a password. It verifies one.
  It runs your input through a mathematical transformation
  and checks the result against a hardcoded checksum.

  Reverse the math. Craft the serial. Unlock the radio.

  Format: XXXX-XXXX-XXXX-XXXX

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MISSION OBJECTIVE:

  Reverse engineer the firmware's key verification algorithm
  to generate a valid serial key in XXXX-XXXX-XXXX-XXXX
  format and capture the flag.

FILES PROVIDED:
  → comms_radio.elf    (Radio firmware binary)

PHANTOM COMMAND ACTUAL — OUT.
