╔══════════════════════════════════════════════════════════════╗
║          OPERATION BLACK VAULT — PHANTOM COMMAND            ║
║                    MISSION BRIEFING                         ║
╚══════════════════════════════════════════════════════════════╝

CLASSIFICATION: UNCLASSIFIED
OPERATION CODENAME: TERMINAL ACCESS
CATEGORY: Reverse Engineering
DIFFICULTY: 🟢 EASY | POINTS: 200

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTELLIGENCE REPORT:

  We recovered an authentication module from a low-level
  Vault guard's terminal. The binary challenges operators
  for an access code before granting entry.

  Our analysts suspect the developer hardcoded the access
  code directly into the binary — thinking that compiled
  machine code was unreadable.

  Strings disagrees. So does Ghidra.
  Find the code. Enter the Vault.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MISSION OBJECTIVE:

  Reverse engineer the authentication binary to extract
  the hardcoded access code and capture the flag.

FILES PROVIDED:
  → auth_module.elf    (Guard terminal binary)

PHANTOM COMMAND ACTUAL — OUT.
